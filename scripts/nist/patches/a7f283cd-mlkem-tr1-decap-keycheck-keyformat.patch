diff --git a/gen-val/src/generation/src/NIST.CVP.ACVTS.Libraries.Generation/ML-KEM/FIPS203/tr1/EncapDecap/TestGroupGeneratorKeyCheckVal.cs b/gen-val/src/generation/src/NIST.CVP.ACVTS.Libraries.Generation/ML-KEM/FIPS203/tr1/EncapDecap/TestGroupGeneratorKeyCheckVal.cs
--- a/gen-val/src/generation/src/NIST.CVP.ACVTS.Libraries.Generation/ML-KEM/FIPS203/tr1/EncapDecap/TestGroupGeneratorKeyCheckVal.cs
+++ b/gen-val/src/generation/src/NIST.CVP.ACVTS.Libraries.Generation/ML-KEM/FIPS203/tr1/EncapDecap/TestGroupGeneratorKeyCheckVal.cs
@@ -2,5 +2,6 @@
 using System.Linq;
 using System.Threading.Tasks;
+using NIST.CVP.ACVTS.Libraries.Crypto.Common.PQC.Enums;
 using NIST.CVP.ACVTS.Libraries.Crypto.Common.PQC.MLKEM;
 using NIST.CVP.ACVTS.Libraries.Generation.Core;
 using NIST.CVP.ACVTS.Libraries.Generation.ML_KEM.FIPS203.tr1.EncapDecap.TestCaseExpectations;
@@ -22,6 +23,7 @@ public class TestGroupGeneratorKeyCheckVal : ITestGroupGeneratorAsync<Parameters
                     TestType = "VAL",
                     Function = MLKEMFunction.DecapsulationKeyCheck,
                     ParameterSet = parameterSet,
+                    KeyFormat = PrivateKeyFormat.Expanded,
                     DecapsulationKeyExpectationProvider = new DecapsulationKeyExpectationProvider()
                 });
             }
diff --git a/gen-val/src/generation/test/NIST.CVP.ACVTS.Libraries.Generation.Tests/ML-KEM/FIPS203/tr1/EncapDecap/TestGroupGeneratorKeyCheckValTests.cs b/gen-val/src/generation/test/NIST.CVP.ACVTS.Libraries.Generation.Tests/ML-KEM/FIPS203/tr1/EncapDecap/TestGroupGeneratorKeyCheckValTests.cs
new file mode 100644
--- /dev/null
+++ b/gen-val/src/generation/test/NIST.CVP.ACVTS.Libraries.Generation.Tests/ML-KEM/FIPS203/tr1/EncapDecap/TestGroupGeneratorKeyCheckValTests.cs
@@ -0,0 +1,30 @@
+using System.Threading.Tasks;
+using NIST.CVP.ACVTS.Libraries.Crypto.Common.PQC.Enums;
+using NIST.CVP.ACVTS.Libraries.Crypto.Common.PQC.MLKEM;
+using NIST.CVP.ACVTS.Libraries.Generation.ML_KEM.FIPS203.tr1.EncapDecap;
+using NIST.CVP.ACVTS.Tests.Core.TestCategoryAttributes;
+using NUnit.Framework;
+
+namespace NIST.CVP.ACVTS.Libraries.Generation.Tests.ML_KEM.FIPS203.tr1.EncapDecap;
+
+[TestFixture, UnitTest]
+public class TestGroupGeneratorKeyCheckValTests
+{
+    [Test]
+    public async Task ShouldUseExpandedKeyFormatForDecapsulationKeyCheck()
+    {
+        var subject = new TestGroupGeneratorKeyCheckVal();
+        var parameters = new Parameters
+        {
+            ParameterSets = [MLKEMParameterSet.ML_KEM_512],
+            Functions = [MLKEMFunction.DecapsulationKeyCheck],
+            KeyFormats = [PrivateKeyFormat.Seed, PrivateKeyFormat.Expanded]
+        };
+
+        var result = await subject.BuildTestGroupsAsync(parameters);
+
+        Assert.That(result, Has.Count.EqualTo(1));
+        Assert.That(result[0].Function, Is.EqualTo(MLKEMFunction.DecapsulationKeyCheck));
+        Assert.That(result[0].KeyFormat, Is.EqualTo(PrivateKeyFormat.Expanded));
+    }
+}
